(()=>{
const area=document.getElementById('calendarArea');if(!area)return;
const META={'중국':{country:'중국',city:'린이',tz:'Asia/Shanghai',cls:'venue-china'},'브라질':{country:'브라질',city:'브라질리아',tz:'America/Sao_Paulo',cls:'venue-brazil'},'캐나다':{country:'캐나다',city:'오타와',tz:'America/Toronto',cls:'venue-canada'},'프랑스':{country:'프랑스',city:'오를레앙',tz:'Europe/Paris',cls:'venue-france'},'폴란드':{country:'폴란드',city:'글리비체',tz:'Europe/Warsaw',cls:'venue-poland'},'슬로베니아':{country:'슬로베니아',city:'류블랴나',tz:'Europe/Ljubljana',cls:'venue-slovenia'},'세르비아':{country:'세르비아',city:'베오그라드',tz:'Europe/Belgrade',cls:'venue-serbia'},'미국':{country:'미국',city:'시카고',tz:'America/Chicago',cls:'venue-usa'},'일본':{country:'일본',city:'오사카',tz:'Asia/Tokyo',cls:'venue-japan'}};
const FLAGS={일본:'jp',우크라이나:'ua',폴란드:'pl',중국:'cn',슬로베니아:'si',쿠바:'cu',튀르키예:'tr',미국:'us',벨기에:'be',브라질:'br',세르비아:'rs',아르헨티나:'ar',이란:'ir',불가리아:'bg',이탈리아:'it',독일:'de',프랑스:'fr',캐나다:'ca'};
const WEEK_PLACES={1:['중국','브라질','캐나다'],2:['프랑스','폴란드','슬로베니아'],3:['세르비아','미국','일본']},WEEK_MONTH={1:0,2:0,3:1};
let matches=[],allTeams=[];
let state={week:0,places:new Set(Object.keys(META)),teams:new Set(),month:0,selectedDay:null,timeMode:'kst',mobilePanelOpen:false,placesOpen:true,teamsOpen:false};
const months=[{year:2026,month:6,days:30},{year:2026,month:7,days:31}];
const venueLabel=p=>META[p]?`${META[p].country} (${META[p].city})`:p;
const flagImg=t=>FLAGS[t]?`<img class="sv3-flag-img" src="https://flagcdn.com/w40/${FLAGS[t]}.png" alt="${t} 국기">`:'<span class="sv3-flag-img"></span>';
const matchUrl=g=>`match.html?id=${encodeURIComponent(g.id)}`;
function localParts(g){const m=META[g.place];if(state.timeMode!=='local'||!m)return{date:g.date,time:g.time};const[y,mo,d]=g.date.split('-').map(Number),[h,min]=g.time.split(':').map(Number),utc=new Date(Date.UTC(y,mo-1,d,h-9,min)),parts=new Intl.DateTimeFormat('en-CA',{timeZone:m.tz,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(utc),get=t=>parts.find(p=>p.type===t)?.value||'';return{date:`${get('year')}-${get('month')}-${get('day')}`,time:`${get('hour')}:${get('minute')}`}}
const displayDate=g=>localParts(g).date,displayTime=g=>localParts(g).time;
const teamAllowed=g=>!state.teams.size||state.teams.has(g.home)||state.teams.has(g.away);
function filteredGames(month,day){const date=`${month.year}-${String(month.month).padStart(2,'0')}-${String(day).padStart(2,'0')}`;return matches.filter(g=>displayDate(g)===date&&(!state.week||g.week===state.week)&&state.places.has(g.place)&&teamAllowed(g))}
const visiblePlaces=()=>state.week?WEEK_PLACES[state.week]:Object.keys(META);
function venueMonth(place){const hit=matches.find(g=>g.place===place);if(!hit)return 0;return (state.timeMode==='local'?localParts(hit).date:hit.date).startsWith('2026-07')?1:0}
function teamMonth(team){const gs=matches.filter(g=>g.home===team||g.away===team);if(!gs.length)return state.month;if(gs.some(g=>Number(displayDate(g).slice(5,7))===months[state.month].month))return state.month;return gs.some(g=>displayDate(g).startsWith('2026-07'))?1:0}
const sectionTitle=(label,key,isOpen)=>`<button class="sv3-section-toggle" data-section="${key}" aria-expanded="${isOpen}"><span>${label}</span><span aria-hidden="true">${isOpen?'▲':'▼'}</span></button>`;
function summaryText(kind,selected,total){if(selected===total)return`✓ 전체 ${kind}`;if(selected===0)return`전체 ${kind}`;return`${kind} ${selected}개 선택`}
function render(){area.innerHTML=`<button class="sv3-mobile-filter" id="sv3FilterToggle">☰ 필터 열기</button><div class="schedule-v3"><aside class="sv3-panel ${state.mobilePanelOpen?'open':''}" id="sv3Panel">${filtersHtml()}</aside><section><div class="sv3-main">${calendarHtml(months[state.month])}</div><div class="sv3-detail">${detailHtml(months[state.month])}</div></section></div>`;bind()}
function filtersHtml(){const weekBtns=[0,1,2,3].map(w=>`<button class="sv3-btn ${state.week===w?'active':''}" data-week="${w}">${w===0?'전체':`${w}주차`}</button>`).join(''),visible=visiblePlaces(),placeRows=visible.map(p=>`<label class="sv3-place-row ${META[p].cls}"><input class="sv3-check" type="checkbox" data-place="${p}" ${state.places.has(p)?'checked':''}><span class="sv3-dot"></span><span class="sv3-place-label">${venueLabel(p)}</span></label>`).join(''),teamRows=allTeams.map(t=>`<label class="sv3-place-row sv3-team-row"><input class="sv3-check" type="checkbox" data-team="${t}" ${state.teams.has(t)?'checked':''}>${flagImg(t)}<span class="sv3-place-label">${t}</span></label>`).join(''),single=state.places.size===1;return `<div class="sv3-filter-title">주차 선택</div><div class="sv3-week-buttons">${weekBtns}</div>${sectionTitle('개최 지역','places',state.placesOpen)}<div class="sv3-collapsible ${state.placesOpen?'open':''}"><button class="sv3-select-all ${state.places.size===visible.length?'active':''}" data-toggle-places>${summaryText('지역',state.places.size,visible.length)}</button><div class="sv3-places">${placeRows}</div></div>${sectionTitle('출전 국가','teams',state.teamsOpen)}<div class="sv3-collapsible ${state.teamsOpen?'open':''}"><button class="sv3-select-all ${state.teams.size===allTeams.length?'active':''}" data-toggle-teams>${summaryText('국가',state.teams.size,allTeams.length)}</button><div class="sv3-places sv3-team-list">${teamRows}</div></div><div class="sv3-filter-title">시간 기준</div><div class="sv3-time-row"><button class="sv3-btn ${state.timeMode==='kst'?'active':''}" data-time="kst">한국시간</button><button class="sv3-btn ${state.timeMode==='local'?'active':''}" data-time="local" ${single?'':'disabled'}>현지시간</button></div><div class="sv3-time-note">${single?'현지시간 선택 시 경기 날짜와 시간 모두 개최지 기준으로 변경됩니다.':'여러 개최지가 선택되어 한국시간으로 고정됩니다.'}</div><div class="sv3-filter-actions"><button class="sv3-done" id="sv3Done">완료</button><button class="sv3-reset" id="sv3Reset">필터 초기화</button></div>`}
function calendarHtml(month){const first=new Date(month.year,month.month-1,1).getDay(),offset=first===0?6:first-1;let cells='';for(let i=0;i<offset;i++)cells+='<div class="sv3-day empty"></div>';for(let d=1;d<=month.days;d++){const games=filteredGames(month,d),sun=(offset+d-1)%7===6,selected=state.selectedDay===d;cells+=`<div class="sv3-day ${sun?'sun':''} ${selected?'selected':''}" data-day="${d}"><div class="sv3-daynum">${d}</div><div class="sv3-games">${games.map(gameMini).join('')}</div>${games.length?`<div class="sv3-count">총 ${games.length}경기</div>`:''}</div>`}return `<div class="sv3-head"><div class="sv3-month-nav"><button id="sv3Prev">‹</button><div class="sv3-month-title">${month.year}년 ${month.month}월</div><button id="sv3Next">›</button></div><div class="sv3-mode">${state.timeMode==='local'?'선택 개최지 현지 날짜·시간':'한국시간(KST) 기준'}</div></div><div class="sv3-calendar">${['월','화','수','목','금','토','일'].map(x=>`<div class="sv3-weekday">${x}</div>`).join('')}${cells}</div><div class="sv3-scroll-hint">날짜를 누르면 아래에서 전체 경기 일정을 확인할 수 있습니다.</div>`}
function gameMini(g){const m=META[g.place]||{},middle=g.score||'vs';return `<a class="sv3-game ${m.cls||''}" href="${matchUrl(g)}" aria-label="${g.home} ${middle} ${g.away} 경기 상세"><div class="sv3-game-line"><span class="sv3-game-time">${displayTime(g)}</span><span class="sv3-game-match">${g.home} ${middle} ${g.away}</span></div><span class="sv3-game-place">${venueLabel(g.place)}</span></a>`}
function detailHtml(month){let day=state.selectedDay;if(!day){day=Array.from({length:month.days},(_,i)=>i+1).find(d=>filteredGames(month,d).length)||1;state.selectedDay=day}const games=filteredGames(month,day),groups={};games.forEach(g=>(groups[g.place]||(groups[g.place]=[])).push(g));return `<div class="sv3-detail-head"><h3>${month.year}-${String(month.month).padStart(2,'0')}-${String(day).padStart(2,'0')} 경기 일정</h3><strong>총 ${games.length}경기</strong></div>${Object.entries(groups).map(([p,gs])=>venueCard(p,gs)).join('')||'<div>선택 조건에 맞는 경기가 없습니다.</div>'}`}
function venueCard(p,gs){const m=META[p]||{};return `<section class="sv3-venue-card ${m.cls||''}"><div class="sv3-venue-title">${venueLabel(p)}</div>${gs.map(g=>`<a class="sv3-match-row" href="${matchUrl(g)}"><time>${displayTime(g)}</time><div class="sv3-result-line"><div class="sv3-team-block">${flagImg(g.home)}<span class="sv3-team-name">${g.home}</span></div><strong class="sv3-result-score">${g.score||'vs'}</strong><div class="sv3-team-block"><span class="sv3-team-name">${g.away}</span>${flagImg(g.away)}</div></div></a>`).join('')}</section>`}
function bind(){document.getElementById('sv3FilterToggle')?.addEventListener('click',()=>{state.mobilePanelOpen=true;render()});document.getElementById('sv3Done')?.addEventListener('click',()=>{state.mobilePanelOpen=false;render()});document.querySelectorAll('[data-section]').forEach(b=>b.onclick=()=>{const k=b.dataset.section;if(k==='places')state.placesOpen=!state.placesOpen;if(k==='teams')state.teamsOpen=!state.teamsOpen;render()});document.querySelectorAll('[data-week]').forEach(b=>b.onclick=()=>{const w=Number(b.dataset.week);state.week=w;state.places=new Set(w?WEEK_PLACES[w]:Object.keys(META));state.timeMode='kst';state.selectedDay=null;if(w&&Number.isInteger(WEEK_MONTH[w]))state.month=WEEK_MONTH[w];render()});document.querySelector('[data-toggle-places]')?.addEventListener('click',()=>{const v=visiblePlaces(),all=v.every(p=>state.places.has(p));v.forEach(p=>all?state.places.delete(p):state.places.add(p));if(state.places.size!==1)state.timeMode='kst';state.selectedDay=null;render()});document.querySelector('[data-toggle-teams]')?.addEventListener('click',()=>{state.teams=state.teams.size===allTeams.length?new Set():new Set(allTeams);state.selectedDay=null;render()});document.querySelectorAll('[data-place]').forEach(i=>i.addEventListener('change',()=>{const p=i.dataset.place;if(i.checked){state.places.add(p);state.month=venueMonth(p)}else state.places.delete(p);if(state.places.size!==1)state.timeMode='kst';state.selectedDay=null;render()}));document.querySelectorAll('[data-team]').forEach(i=>i.addEventListener('change',()=>{const t=i.dataset.team;if(i.checked){state.teams.add(t);state.month=teamMonth(t)}else state.teams.delete(t);state.selectedDay=null;render()}));document.querySelectorAll('[data-time]').forEach(b=>b.onclick=()=>{if(!b.disabled){state.timeMode=b.dataset.time;if(state.timeMode==='local'&&state.places.size===1)state.month=venueMonth([...state.places][0]);state.selectedDay=null;render()}});document.querySelectorAll('.sv3-day[data-day]').forEach(d=>d.onclick=e=>{if(e.target.closest('a'))return;state.selectedDay=Number(d.dataset.day);render()});document.getElementById('sv3Prev').onclick=()=>{state.month=(state.month-1+months.length)%months.length;state.selectedDay=null;render()};document.getElementById('sv3Next').onclick=()=>{state.month=(state.month+1)%months.length;state.selectedDay=null;render()};document.getElementById('sv3Reset').onclick=()=>{state={week:0,places:new Set(Object.keys(META)),teams:new Set(allTeams),month:0,selectedDay:null,timeMode:'kst',mobilePanelOpen:true,placesOpen:true,teamsOpen:false};render()}}
fetch('data/matches/vnl-2026-calendar.json?v=20260715-1',{cache:'no-store'}).then(r=>r.json()).then(data=>{matches=(data.matches||[]).map((x,i)=>({id:`KVL-M-${String(i+1).padStart(6,'0')}`,date:x[0],time:x[1],week:x[2],home:x[3],away:x[4],score:x[5],place:x[6]}));allTeams=[...new Set(matches.flatMap(g=>[g.home,g.away]))].sort((a,b)=>a.localeCompare(b,'ko'));state.teams=new Set(allTeams);render()}).catch(()=>{area.innerHTML='<div class="sv3-detail">일정 데이터를 불러오지 못했습니다.</div>'});
})();