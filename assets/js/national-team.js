(()=>{const root=document.querySelector('[data-national-team]');if(!root)return;const cfg=JSON.parse(root.dataset.nationalTeam);const el=id=>document.getElementById(id);const countLabel=v=>Number.isFinite(v)?`${v}명`:'확인 중';document.title=`${cfg.name} Active Entry | K-Volley Lab`;el('ntFlag').src=cfg.flag;el('ntFlag').alt=`${cfg.name} flag`;el('ntTitle').textContent=`${cfg.name} Active Entry`;el('ntLead').textContent=cfg.lead;el('ntCoach').textContent=cfg.coach||'확인 중';el('ntActive').textContent=countLabel(cfg.active);el('ntReserve').textContent=countLabel(cfg.reserve);el('ntTotal').textContent=Number.isFinite(cfg.active)&&Number.isFinite(cfg.reserve)?`${cfg.active+cfg.reserve}명`:'자동 계산';el('ntSchedule').href=`schedules.html?tournament=vnl&team=${encodeURIComponent(cfg.nameKo)}`;
const aliases={Türkiye:['Türkiye','Turkey'],USA:['USA','United States']};
const matchCountry=p=>{const names=aliases[cfg.countryKey]||[cfg.countryKey,cfg.name];return names.includes(p.country)||p.countryKo===cfg.nameKo};
Promise.all([
 fetch('data/entries/vnl-2026-men-v25.json',{cache:'no-store'}).then(r=>r.ok?r.json():{players:[]}).catch(()=>({players:[]})),
 fetch(cfg.dataSource||'players.json',{cache:'no-store'}).then(r=>r.ok?r.json():[]).catch(()=>([]))
]).then(([master,fallback])=>{const masterRows=(master.players||[]).filter(matchCountry);if(masterRows.length){const coach=masterRows.find(p=>p.coach)?.coach;if(coach)el('ntCoach').textContent=coach;render(masterRows);return}const raw=Array.isArray(fallback)?fallback:fallback.players||[];const keys=cfg.countryKeys||[cfg.countryKey].filter(Boolean);render(keys.length?raw.filter(p=>keys.includes(p.country)):raw)});
function render(list){const active=list.filter(p=>String(p.entryStatus||p.rosterStatus||'Active').toLowerCase()!=='reserve');const reserve=list.filter(p=>String(p.entryStatus||p.rosterStatus||'').toLowerCase()==='reserve');el('ntActive').textContent=`${active.length}명`;el('ntReserve').textContent=`${reserve.length}명`;el('ntTotal').textContent=`${list.length}명`;draw('ntActiveBody',active,'Active');draw('ntReserveBody',reserve,'Reserve');el('ntActiveCount').textContent=`${active.length}명`;el('ntReserveCount').textContent=`${reserve.length}명`;el('ntReserveSection').hidden=reserve.length===0}
function draw(id,list,fallback){const tbody=el(id);if(!list.length){tbody.innerHTML=`<tr><td colspan="8" class="nt-empty">${fallback} 선수 없음</td></tr>`;return}tbody.innerHTML=[...list].sort((a,b)=>(Number(a.number)||999)-(Number(b.number)||999)).map(p=>{const status=p.entryStatus||p.rosterStatus||fallback;const cls=String(status).toLowerCase()==='reserve'?'reserve':'active';const src=p.officialSource||p.vnlProfile||'';const name=p.name||p.fullName||'';const nameKo=p.nameKo||p.korean||'';return `<tr><td>${p.number??''}</td><td>${p.player_id?`<a class="nt-player-link" href="player.html?id=${encodeURIComponent(p.player_id)}">${name}</a>`:name}</td><td>${nameKo}</td><td><span class="nt-pos">${p.position||''}</span></td><td>${p.height?`${p.height}cm`:''}</td><td>${p.dob||''}</td><td><span class="nt-status ${cls}">${cls==='reserve'?'Reserve':'Active'}</span></td><td>${src?`<a class="nt-source" href="${src}" target="_blank" rel="noopener">Official</a>`:'—'}</td></tr>`}).join('')}}})();